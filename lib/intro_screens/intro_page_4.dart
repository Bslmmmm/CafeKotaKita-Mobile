import 'package:flutter/material.dart';

class IntroPage4 extends StatelessWidget {
  const IntroPage4({Key? key}) : super(key: key); // Tambahkan ini

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
